import { useBalance } from 'wagmi'
import contract from "asset/contracts.json";

/*function getPaymentTokenBalance(wallet) {
 return useBalance({address: wallet, token: contract.paymentToken})
}*/

export {
    // getPaymentTokenBalance
}